projection;
strict ( 2 );
use draft;

define behavior for Z_C_PRODUCT_OD alias Product
{
  use create;
  use update;
  use delete;


  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}